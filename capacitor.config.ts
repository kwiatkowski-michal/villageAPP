import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'village.app.ionic7',
  appName: 'village-app-ionic7',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
